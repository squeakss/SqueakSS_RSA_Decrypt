# List of ASCII values
ascii_values = [, , , , , , , , , , , , ]

# Decode ASCII values to characters and join them into a string
decoded_string = ''.join(chr(value) for value in ascii_values)

print(decoded_string)